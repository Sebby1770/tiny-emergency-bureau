/* Bureau of Tiny Emergencies — optional Supabase leaderboard config.
   Leave empty strings to disable remote leaderboard (local fallback only). */
window.BUREAU_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};